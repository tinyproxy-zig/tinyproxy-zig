## 1. Implementation
- [x] 1.1 Add `src/http.zig` with `RequestLine`, `HttpMessage`, `BodyReader` and tests
- [x] 1.2 Extend `src/buffer.zig` `LineReader` with `read`/`read_exact` and tests
- [x] 1.3 Update `src/request.zig` to use HTTP parser and stream request bodies
- [x] 1.4 Register `src/http.zig` tests in `build.zig`
